# 12-8
    class Kata {

    fun makeNegative(x: Int): Int {
        var x = x
        if (x > 0) {
            x = x*-1
        }
        else {
            x = x*1
        }
    return x
    }
    }
